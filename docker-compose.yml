version: "2"
services:
    service-a:
        image: localhost:5000/sample-app/service-a
        environment:
            - APPINSIGHTS_INSTRUMENTATIONKEY
        ports:
            - "8080:80"
        links:
            - service-b
    
    service-b:
        image: localhost:5000/sample-app/service-a
        environment:
            - APPINSIGHTS_INSTRUMENTATIONKEY
        expose:
            - 80
